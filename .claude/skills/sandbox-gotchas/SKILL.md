---
name: sandbox-gotchas
description: Known-to-bite sandbox / tooling issues in this repo — git mv cross-device failures, the wasm pkg-not-found install loop, the broken build:core script, missing wasm-pack, the deleted Playwright e2e. TRIGGER when you hit `ERR_PNPM_WORKSPACE_PKG_NOT_FOUND`, `Invalid cross-device link`, "wasm-pack: command not found", `pnpm test:e2e` failures, `build:core` errors, or unexplained pnpm install failures around the harmoni-wasm package. SKIP unless you've already hit one of these symptoms.
---

# Sandbox gotchas

## `git mv` fails with `Invalid cross-device link`

The sandbox crosses device boundaries when moving files. Plain `mv`
followed by `git add -A` works — git's rename detection picks the
move up at add time.

## `pnpm install` fails after renaming the wasm crate

The `apps/web` package depends on `harmoni-wasm: workspace:*`, which
resolves to a workspace link at `crates/harmoni-wasm/pkg`. The
`pkg/` directory is generated by `wasm-pack build` and is **not
tracked in git**.

If you rename the crate (or `pkg/` is otherwise missing),
`pnpm install` fails with `ERR_PNPM_WORKSPACE_PKG_NOT_FOUND` until
either:

1. `pnpm run build:wasm` regenerates the pkg under the new name, or
2. You hand-edit `pnpm-lock.yaml` to rename the importer path and
   the workspace link. Confirm with `pnpm install --frozen-lockfile`.

Option 2 is only worth it if wasm-pack isn't available — see next.

## `wasm-pack` may not be installed

If `pnpm run build:wasm` fails with "wasm-pack: command not found",
the sandbox doesn't have the tool. You can't regenerate the `.d.ts`
without it. The current structs are field-for-field identical
post-Step A, so the output *should* be byte-equivalent — but verify
manually when wasm-pack is available.

## `build:core` pnpm script is broken

Root `package.json` has:

```
"build:core": "pnpm --filter harmoni-core exec cargo build"
```

`harmoni-core` is a Rust crate, not a pnpm workspace package, so the
filter doesn't match. Pre-existing breakage. Use `cargo build -p
harmoni-core` from the workspace root instead. Don't fix the script
unless asked.

## `pnpm test:e2e` will fail

The Playwright e2e test was deleted because it targeted a
contrast-preview UI that no longer exists in `App.tsx`. The
`test:e2e` script remains in root `package.json` and would fail
with "no tests found" if run. Not yet replaced.

## Don't run filesystem-scope commands from `/`

`find` from `/` exhausts resources on large trees. Search from `.`
or a specific subtree (`find packages/`, `find crates/`).
