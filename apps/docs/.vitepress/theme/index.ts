import DefaultTheme from "vitepress/theme";
import "./custom.css";

// Extends the default VitePress theme with Primitiv branding (fonts + logo
// handling) defined in custom.css. No component overrides for v1.
export default DefaultTheme;
