[package]
name = "primitiv-emit"
version = "0.1.0"
edition = "2024"

[dependencies]
harmoni-core = { path = "../harmoni-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
pretty_assertions = "1"
